trigger:
  branches:
    include:
      - development
  paths:
    include:
      - src/ui-spa/*

pr: none

stages:
  - template: ../templates/ui-build-and-deploy-stages.yml
    parameters:
      environment: "dev"
